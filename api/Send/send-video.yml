openapi: 3.0.0
info:
  title: "Enviar video"
  description: "Envía un video o archivo multimedia a través del sistema."
  version: 1.0.0

servers:
  - url: https://{tenant_id}.qr.buho.la
    description: Tenant Server
    variables:
      tenant_id:
        default: demo
        description: Tenant Subdomain

paths:
  "/api/send/{instance}":
    post:
      summary: "Enviar video"
      description: "Envía un video (PTV o tradicional) a través de la API de Evolution."
      parameters:
        - name: instance
          in: path
          required: true
          description: Número de dispositivo
          schema:
            type: string
            example: "51987654321"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - type
                - payload
              properties:
                type:
                  type: string
                  example: "sendMedia"
                payload:
                  type: object
                  required:
                    - number
                    - mediatype
                    - media
                  properties:
                    number:
                      type: string
                      example: "51987654321"
                    mediatype:
                      type: string
                      example: "video"
                    media:
                      type: string
                      example: "https://example.com/video.mp4"
                    caption:
                      type: string
                      example: "Video de prueba"
                    mimetype:
                      type: string
                      example: "video/mp4"
                    filename:
                      type: string
                      example: "video.mp4"
                    delay:
                      type: integer
                      example: 1200
            example:
              type: sendMedia
              payload:
                number: "51987654321"
                mediatype: "video"
                media: "https://example.com/video.mp4"
                caption: "Video de prueba"
                mimetype: "video/mp4"
                filename: "video.mp4"
                delay: 1200
      responses:
        '200':
          description: "Mensaje de video enviado exitosamente"
          content:
            application/json:
              schema:
                type: object
                example:
                  key:
                    remoteJid: "51987654321@s.whatsapp.net"
                    fromMe: true
                    id: "3EB0123456789ABCDEF"
                  status: "PENDING"